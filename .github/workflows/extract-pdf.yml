name: Extract & Validate PDF Data (Visual Pipeline)

on:
  workflow_dispatch:
    inputs:
      fix:
        description: 'Auto-corregir datos si hay discrepancias'
        required: false
        default: 'false'
      region:
        description: 'Procesar solo una region (slug, ej: aysen). Vacio = todas.'
        required: false
        default: ''
      force_download:
        description: 'Re-descargar PDFs aunque ya existan'
        required: false
        default: 'false'
  schedule:
    - cron: '0 14 15 2 *'    # 15 febrero 14:00 UTC — las REX se modifican ANTES del inicio de clases (caso Aysen REX 632)
    - cron: '0 14 15 5 *'    # 15 mayo 14:00 UTC — verificacion primer semestre
    - cron: '0 14 31 12 *'   # 31 diciembre 14:00 UTC — resoluciones Mineduc ya publicadas

jobs:
  extract:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '20'

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install PyMuPDF
        run: pip install PyMuPDF

      - name: Install OCR tools
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq poppler-utils tesseract-ocr tesseract-ocr-spa

      # Los PDFs estan gitignoreados y el runner parte sin ellos — este paso
      # era el eslabon faltante (fallo del 15-may: "Generate PNGs" sin PDFs).
      # Las URLs oficiales por region viven en pages.json → resolucion.url.
      - name: Download regional PDFs (from pages.json resolucion.url)
        run: |
          mkdir -p data/extraction-tests
          node -e '
            var fs = require("fs");
            var pages = JSON.parse(fs.readFileSync("data/pages.json", "utf8"));
            var cmds = [];
            pages.forEach(function (p) {
              if (p.resolucion && p.resolucion.url) {
                cmds.push([p.regionSlug, p.resolucion.url]);
              }
            });
            fs.writeFileSync("/tmp/pdf-urls.txt", cmds.map(function (c) { return c.join(" "); }).join("\n"));
            console.log(cmds.length + " URLs de REX regionales");
          '
          FAILED=0
          while read -r SLUG URL; do
            if [ -n "${{ github.event.inputs.region }}" ] && [ "$SLUG" != "${{ github.event.inputs.region }}" ]; then continue; fi
            DEST="data/extraction-tests/mineduc-${SLUG}.pdf"
            if [ -f "$DEST" ] && [ "${{ github.event.inputs.force_download }}" != "true" ]; then continue; fi
            echo "Descargando $SLUG..."
            curl -fsSL --retry 3 --retry-delay 5 -o "$DEST" "$URL" || { echo "FALLO descarga $SLUG"; FAILED=$((FAILED+1)); }
          done < /tmp/pdf-urls.txt
          echo "Descargas fallidas: $FAILED"
          # No abortar por descargas fallidas individuales: el validador
          # reportara las regiones sin datos y la alerta se disparara.

      - name: Generate PNGs from PDFs
        run: python scripts/pdf-to-png.py --all --output-dir=data/snapshots

      - name: Organize table PNGs
        run: node scripts/organize-snapshots.js

      - name: Extract data from PNGs (visual)
        id: extract
        env:
          EXTRACTION_API: ${{ secrets.EXTRACTION_API || 'gemini' }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
        run: |
          ARGS=""
          if [ -n "${{ github.event.inputs.region }}" ]; then ARGS="$ARGS --region=${{ github.event.inputs.region }}"; fi
          node scripts/extract-visual.js $ARGS
        continue-on-error: true

      - name: Validate extraction
        id: validate
        run: node scripts/validate-extraction.js --strict

      - name: Commit snapshots and report
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Agregar PNGs de tablas y manifest
          git add data/snapshots/*.png 2>/dev/null || true
          git add data/snapshots/png-manifest.json 2>/dev/null || true
          git add data/extraction-validation-report.json 2>/dev/null || true

          # Si --fix fue usado, agregar datos corregidos
          if [ "${{ github.event.inputs.fix }}" = "true" ]; then
            git add data/calendar-config.json data/pages.json 2>/dev/null || true
          fi

          git diff --staged --quiet || git commit -m "chore: update visual extraction report [skip ci]"
          git pull --rebase --autostash origin main
          git push

      - name: Regenerate if fixed
        if: github.event.inputs.fix == 'true' && steps.extract.outcome == 'success'
        run: |
          node scripts/generate-pages.js
          node scripts/validate.js

          git add public/ data/
          git diff --staged --quiet || git commit -m "chore: regenerate pages from visual-corrected data [skip ci]"
          git pull --rebase --autostash origin main
          git push

      # Alerta via issue + GITHUB_TOKEN (patrón probado de sync-deploy.yml).
      # Antes: curl-Telegram con secrets inexistentes, condicionado a
      # steps.extract.outcome (con continue-on-error) → quedaba SKIPPED en
      # casi cualquier fallo real (p.ej. el del 15-may fue en Generate PNGs).
      - name: Alert on failure (issue)
        if: failure() || steps.extract.outcome == 'failure'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          MISMATCHES=$(node -e "
            var fs = require('fs');
            try {
              var r = JSON.parse(fs.readFileSync('./data/extraction-validation-report.json', 'utf8'));
              if (!r.summary || r.summary.total_errors === 0) process.exit(1);
              var failedRegions = Object.keys(r.per_region || {}).filter(function(k) { return !r.per_region[k].passed; });
              failedRegions.slice(0, 10).forEach(function(region) {
                var errs = r.per_region[region].errors || [];
                errs.forEach(function(e) { console.log('- ' + region + ': ' + e); });
              });
              if (failedRegions.length > 10) console.log('... y ' + (failedRegions.length - 10) + ' regiones mas');
            } catch(e) { process.exit(1); }
          " 2>/dev/null) || true

          BODY="El pipeline de extracción visual de PDFs Mineduc falló el $(date -u +%Y-%m-%d\ %H:%M) UTC.

          ${MISMATCHES:-Sin reporte de discrepancias disponible — el fallo fue antes de la validación (revisar el log).}

          Log del run: $RUN_URL"
          EXISTING=$(gh issue list --repo "${{ github.repository }}" --state open --search "ALERTA extraccion PDF in:title" --json number --jq '.[0].number // empty')
          if [ -n "$EXISTING" ]; then
            gh issue comment "$EXISTING" --repo "${{ github.repository }}" --body "$BODY"
          else
            gh issue create --repo "${{ github.repository }}" \
              --title "ALERTA extraccion PDF fallo $(date -u +%Y-%m-%d)" \
              --body "$BODY"
          fi

      - name: Summary
        if: always()
        run: |
          node -e "
            var fs = require('fs');
            try {
              var r = JSON.parse(fs.readFileSync('./data/extraction-validation-report.json', 'utf8'));
              console.log('=== VISUAL PIPELINE EXTRACTION REPORT ===');
              console.log('Regiones verificadas: ' + r.summary.regions_checked);
              console.log('Regiones OK: ' + r.summary.regions_passed);
              console.log('Regiones fallidas: ' + r.summary.regions_failed);
              console.log('Total errores: ' + r.summary.total_errors);
              console.log('Total advertencias: ' + r.summary.total_warnings);
              var groups = r.checks.cross_region.groups_detected;
              Object.keys(groups).forEach(function(g) {
                console.log('  Grupo ' + g + ': ' + groups[g].join(', '));
              });
            } catch(e) { console.log('No se pudo leer el reporte: ' + e.message); }
          "
