---
phase: 02-datos-completos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extract-visual.js
  - data/pages.json
  - scripts/generate-pages.js
  - scripts/validate.js
autonomous: true
requirements: [DATA-01, DATA-04]
must_haves:
  truths:
    - "pages.json contains finAnoSinJEC, finAnoEPJA, cierreActas4Medio, diaProfesor, inicioSegundoSemestre for each of the 16 regions"
    - "Data comes from gold standard / visual extraction pipeline, not hardcoded"
    - "generate-pages.js passes the 5 new fields into template replacements"
    - "validate.js does not error on the new fields (not required, but if present must be non-empty strings)"
  artifacts:
    - path: "data/pages.json"
      provides: "16 regions with 5 additional fields each"
      contains: "finAnoSinJEC"
    - path: "scripts/extract-visual.js"
      provides: "Local mode maps 5 additional milestones from extraction"
      contains: "finAnoSinJEC"
    - path: "scripts/generate-pages.js"
      provides: "Passes new fields to template"
      contains: "finAnoSinJEC"
  key_links:
    - from: "scripts/extract-visual.js"
      to: "data/visual-extraction.json"
      via: "buildFromLocalData maps gold standard milestones to 5 new fields"
      pattern: "finAnoSinJEC|cierreActas4Medio|diaProfesor"
    - from: "scripts/generate-pages.js"
      to: "data/pages.json"
      via: "reads pages.json and replaces {{placeholders}} including new fields"
      pattern: "finAnoSinJEC|inicioSegundoSemestre"
---

<objective>
Add the 5 additional calendar milestone fields to the data pipeline: extract them from visual-extraction.json, store in pages.json, and wire through generate-pages.js.

Purpose: DATA-01 requires pages.json to have finAnoSinJEC, finAnoEPJA, cierreActas4Medio, diaProfesor, inicioSegundoSemestre. DATA-04 requires these come from the visual pipeline, not hardcoded.

Output: pages.json with 5 new fields per region, extract-visual.js --local producing them, generate-pages.js passing them to templates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-datos-completos/02-CONTEXT.md
@data/pages.json
@scripts/extract-visual.js
@scripts/generate-pages.js
@scripts/validate.js
@data/extraction-tests/metropolitana-gold-standard.json

<interfaces>
<!-- From data/extraction-tests/metropolitana-gold-standard.json — the gold standard milestones that must be mapped: -->

semestral milestones with labels:
- "Inicio de clases segundo semestre" -> date: "2026-07-06" (maps to inicioSegundoSemestre)
- "Dia del profesor" -> date: "2026-10-16" (maps to diaProfesor)
- "Cierre actas 4 Medio" -> date: "2026-11-20" (maps to cierreActas4Medio)
- "Ultimo dia de clases establecimientos con JEC (38 semanas)" -> date: "2026-12-04" (already mapped as finAno)
- "Ultimo dia de clases establecimiento sin JEC (40 semanas)" -> date: "2026-12-18" (maps to finAnoSinJEC)
- "Ultimo dia de clases EPJA (36 semanas)" -> date: "2026-11-20" (maps to finAnoEPJA)

<!-- From data/pages.json — current structure per region: -->
{
  "slug": "region/metropolitana",
  "region": "Metropolitana",
  "regionSlug": "metropolitana",
  "inicio": "4 de marzo",
  "vacacionesInicio": "22 de junio",
  "vacacionesFin": "3 de julio",
  "fiestasPatriasInicio": "14 de septiembre",
  "fiestasPatriasFin": "18 de septiembre",
  "finAno": "4 de diciembre",
  "diasVacacionesInvierno": "12",
  "diasFiestasPatrias": "5"
}

<!-- From scripts/validate.js line 98-103 — REQUIRED_FIELDS array: -->
var REQUIRED_FIELDS = [
  'slug', 'region', 'regionSlug',
  'inicio', 'vacacionesInicio', 'vacacionesFin',
  'fiestasPatriasInicio', 'fiestasPatriasFin',
  'finAno', 'diasVacacionesInvierno', 'diasFiestasPatrias',
  'title', 'description', 'priority'
];

<!-- From scripts/generate-pages.js line 79-87 — template replacement logic: -->
Object.keys(page).forEach(function (key) {
  var re = new RegExp('\\{\\{' + key + '\\}\\}', 'g');
  html = html.replace(re, page[key] || '');
});
<!-- Note: generate-pages.js already replaces ALL keys from page object, so adding fields to pages.json automatically makes them available as {{fieldName}} in the template. No code change needed in generate-pages.js for template replacement. -->

<!-- From scripts/generate-pages.js line 124-136 — regions-data.js generation: -->
regionsData[page.regionSlug] = {
  name: page.region,
  inicio: page.inicio,
  vacIni: page.vacacionesInicio,
  vacFin: page.vacacionesFin,
  fpIni: page.fiestasPatriasInicio,
  fpFin: page.fiestasPatriasFin,
  fin: page.finAno,
  diasVac: page.diasVacacionesInvierno,
  diasFP: page.diasFiestasPatrias
};
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 5 additional fields from visual pipeline into pages.json</name>
  <files>scripts/extract-visual.js, data/pages.json, data/visual-extraction.json</files>
  <read_first>
    - data/extraction-tests/metropolitana-gold-standard.json (milestone labels and dates for mapping)
    - data/extraction-tests/TODAS-REGIONES-visual-extraction.json (current flat format — source of truth)
    - scripts/extract-visual.js (buildFromLocalData function starting ~line 416)
    - data/pages.json (current structure)
    - data/visual-extraction.json (current output format)
  </read_first>
  <action>
    **Step A — Update extract-visual.js `buildFromLocalData()` function (around line 416-522):**

    The gold standard (metropolitana-gold-standard.json) shows these additional milestones exist in semestral array. The --local mode currently only maps the 6 core fields. Add 5 new milestone hitos to the semestral array in `buildFromLocalData()`:

    1. "Inicio de clases segundo semestre" — date after winter recess ends. For ESTANDAR regions: July 6. For NORTE: July 27. For SUR: varies. Calculate as: day after vacacionesFin (parse vacacionesFin, add 2 days to get Monday).
    2. "Dia del profesor" — October 16 for all regions (national date, per gold standard).
    3. "Cierre actas 4 Medio" — November 20 for all regions (per gold standard).
    4. "Ultimo dia de clases establecimiento sin JEC (40 semanas)" — December 18 for ESTANDAR/NORTE. For SUR/SUR-PARCIAL check if different.
    5. "Ultimo dia de clases EPJA (36 semanas)" — November 20 for all regions (per gold standard).

    Add these 5 hitos AFTER the existing "Ultimo dia de clases establecimientos con JEC" entry in the semestral array. Use the same object structure: `{ label, date, raw_text, day_of_week }`.

    For calculating "inicio segundo semestre": parse `vacacionesFin` using existing `parseDateES()`, add 2 days to get Monday (the first school day after winter recess ends on Friday). Use a simple date arithmetic helper.

    **Step B — Create a new script `scripts/populate-pages-json.js` that reads visual-extraction.json and updates pages.json with the 5 new fields:**

    This script maps from the semestral milestones to the 5 flat field names:
    - Find milestone with label containing "sin JEC" -> extract date -> format as "DD de mes" -> set `finAnoSinJEC`
    - Find milestone with label containing "EPJA" -> extract date -> format as "DD de mes" -> set `finAnoEPJA`
    - Find milestone with label containing "Cierre actas" or "cierre actas" -> extract date -> format -> set `cierreActas4Medio`
    - Find milestone with label containing "profesor" or "Profesor" -> extract date -> format -> set `diaProfesor`
    - Find milestone with label containing "segundo semestre" or "Inicio.*segundo" -> extract date -> format -> set `inicioSegundoSemestre`

    Date format: same as existing fields in pages.json — "DD de mes" (e.g., "18 de diciembre", "16 de octubre"). Use the MONTHS_ES array and format function.

    If a milestone is not found for a region, set the field to "Sin datos".

    Script usage: `node scripts/populate-pages-json.js`
    Reads: data/visual-extraction.json
    Writes: data/pages.json (preserves all existing fields, adds 5 new ones)

    **Step C — Run the pipeline:**
    1. `node scripts/extract-visual.js --local` to regenerate visual-extraction.json with the 5 new milestones
    2. `node scripts/populate-pages-json.js` to update pages.json
    3. Verify pages.json has 16 regions each with the 5 new fields

    **Step D — Update generate-pages.js regions-data.js generation (line 124-136):**

    Add the 5 new fields to the regionsData object so they are available in window.REGIONS_DATA:
    ```
    finSinJEC: page.finAnoSinJEC,
    finEPJA: page.finAnoEPJA,
    cierreActas: page.cierreActas4Medio,
    diaProf: page.diaProfesor,
    ini2doSem: page.inicioSegundoSemestre
    ```

    Note: Template replacement already works for ALL keys in page object (line 84-87 of generate-pages.js), so {{finAnoSinJEC}} etc. will automatically be available in template.html. No change needed there.
  </action>
  <verify>
    <automated>node scripts/extract-visual.js --local && node scripts/populate-pages-json.js && node -e "var p=JSON.parse(require('fs').readFileSync('data/pages.json','utf8')); var ok=p.every(function(r){return r.finAnoSinJEC && r.finAnoEPJA && r.cierreActas4Medio && r.diaProfesor && r.inicioSegundoSemestre}); console.log('All 16 regions have 5 new fields:', ok); if(!ok) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - grep "finAnoSinJEC" data/pages.json returns 16 matches (one per region)
    - grep "finAnoEPJA" data/pages.json returns 16 matches
    - grep "cierreActas4Medio" data/pages.json returns 16 matches
    - grep "diaProfesor" data/pages.json returns 16 matches
    - grep "inicioSegundoSemestre" data/pages.json returns 16 matches
    - grep "finAnoSinJEC" scripts/extract-visual.js returns at least 1 match
    - node scripts/extract-visual.js --local exits 0
    - Metropolitana has finAnoSinJEC="18 de diciembre", diaProfesor="16 de octubre", cierreActas4Medio="20 de noviembre", finAnoEPJA="20 de noviembre", inicioSegundoSemestre="6 de julio"
  </acceptance_criteria>
  <done>pages.json has 5 new fields for all 16 regions, sourced from visual-extraction pipeline (DATA-01, DATA-04 satisfied)</done>
</task>

<task type="auto">
  <name>Task 2: Update validate.js to handle new optional fields and run full build</name>
  <files>scripts/validate.js</files>
  <read_first>
    - scripts/validate.js (full file — understand REQUIRED_FIELDS and validation logic)
    - data/pages.json (verify new fields are present after Task 1)
  </read_first>
  <action>
    **Step A — Add optional field validation in validate.js:**

    After the REQUIRED_FIELDS check loop (around line 119), add a new validation block for the 5 optional fields. These fields are NOT required (to avoid breaking builds if extraction misses them), but if present they must be valid date strings matching the pattern "DD de mes" (e.g., "18 de diciembre") or "Sin datos":

    ```javascript
    var OPTIONAL_DATE_FIELDS = [
      'finAnoSinJEC', 'finAnoEPJA', 'cierreActas4Medio',
      'diaProfesor', 'inicioSegundoSemestre'
    ];

    OPTIONAL_DATE_FIELDS.forEach(function (field) {
      if (page[field] && page[field] !== 'Sin datos') {
        // Validate format: "DD de mes"
        if (!/^\d{1,2}\s+de\s+[a-záéíóúñ]+$/i.test(page[field])) {
          warn('pages.json[' + id + ']: campo "' + field + '" formato inesperado: ' + page[field]);
        }
      }
    });
    ```

    Use `warn()` not `error()` — these are optional fields so format issues are warnings, not build-blockers.

    **Step B — Run full build to verify nothing breaks:**
    1. `node scripts/generate-pages.js` — regenerate all pages
    2. `node scripts/validate.js` — must exit 0 with no errors

    **Step C — Verify health.json reflects correct state:**
    Check that `public/health.json` has status "ok" and regionsCount 16.
  </action>
  <verify>
    <automated>node scripts/generate-pages.js && node scripts/validate.js && node -e "var h=JSON.parse(require('fs').readFileSync('public/health.json','utf8')); console.log('status:', h.status, 'regions:', h.regionsCount); if(h.status!=='ok'||h.regionsCount!==16) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - grep "OPTIONAL_DATE_FIELDS" scripts/validate.js returns at least 1 match
    - grep "finAnoSinJEC" scripts/validate.js returns at least 1 match
    - node scripts/validate.js exits 0
    - node scripts/generate-pages.js exits 0 with "Generadas 16 paginas"
    - public/health.json contains "status": "ok"
  </acceptance_criteria>
  <done>validate.js handles new optional fields gracefully, full build pipeline passes without errors</done>
</task>

</tasks>

<verification>
1. `node scripts/extract-visual.js --local` exits 0 and produces visual-extraction.json with additional milestones
2. `node scripts/populate-pages-json.js` exits 0 and updates pages.json with 5 new fields
3. `node scripts/generate-pages.js` exits 0 — all 16 region pages generated
4. `node scripts/validate.js` exits 0 — no errors
5. `grep -c "finAnoSinJEC" data/pages.json` returns 16
6. `grep -c "diaProfesor" data/pages.json` returns 16
</verification>

<success_criteria>
- pages.json has finAnoSinJEC, finAnoEPJA, cierreActas4Medio, diaProfesor, inicioSegundoSemestre for all 16 regions
- Data provenance: fields derived from visual-extraction.json milestones, not hardcoded
- Full build pipeline (generate + validate) passes cleanly
- New fields follow same date format as existing fields ("DD de mes")
</success_criteria>

<output>
After completion, create `.planning/phases/02-datos-completos/02-01-SUMMARY.md`
</output>
