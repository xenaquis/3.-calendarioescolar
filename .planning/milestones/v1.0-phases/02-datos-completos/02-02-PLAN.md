---
phase: 02-datos-completos
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - data/template.html
  - public/css/components.css
  - data/afirmaciones.json
autonomous: false
requirements: [DATA-02, DATA-03]
must_haves:
  truths:
    - "Region pages show a collapsible section with 5 additional calendar dates below the main table"
    - "The section is closed by default and opens on click"
    - "The visual style is consistent with the existing table and card design"
    - "When a field has no data, the row shows 'Sin datos' instead of being hidden"
  artifacts:
    - path: "data/template.html"
      provides: "Collapsible details section with 5 additional date rows"
      contains: "finAnoSinJEC"
    - path: "public/css/components.css"
      provides: "Styles for the details/summary collapsible section"
      contains: "details-extra"
  key_links:
    - from: "data/template.html"
      to: "data/pages.json"
      via: "{{finAnoSinJEC}} and other placeholders replaced by generate-pages.js"
      pattern: "finAnoSinJEC|cierreActas4Medio|diaProfesor"
    - from: "data/template.html"
      to: "public/css/components.css"
      via: "CSS class references for collapsible section styling"
      pattern: "details-extra"
---

<objective>
Add a collapsible "Mas fechas del calendario escolar" section to the region template with the 5 additional date fields, styled consistently with existing design.

Purpose: DATA-02 requires a collapsible table showing additional dates. DATA-03 requires consistent aesthetics with existing design (same cards, typography, visual hierarchy).

Output: Updated template.html with `<details>` section, updated components.css with collapsible styles, regenerated region pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-datos-completos/02-CONTEXT.md
@.planning/phases/02-datos-completos/02-01-SUMMARY.md
@data/template.html
@public/css/components.css
@data/afirmaciones.json

<interfaces>
<!-- From 02-CONTEXT.md — locked decisions: -->
- Additional data goes below main table in a collapsible <details> section (secondary, not prominent)
- Section closed by default
- Label: "Mas fechas del calendario escolar"
- Each additional date shows day-of-week matching resolution format
- Same .section container + simple table like main calendar table
- Small text note after main table: "Ver mas fechas" as subtle nudge

<!-- From data/pages.json — the 5 new fields available as {{placeholders}} (added by Plan 01): -->
- {{finAnoSinJEC}} — e.g., "18 de diciembre"
- {{finAnoEPJA}} — e.g., "20 de noviembre"
- {{cierreActas4Medio}} — e.g., "20 de noviembre"
- {{diaProfesor}} — e.g., "16 de octubre"
- {{inicioSegundoSemestre}} — e.g., "6 de julio"

<!-- From data/template.html — insertion point (after line 216, after </section> of main table): -->
Line 170-216: Main calendar table section
Line 218-226: Ad slot (mid)
The new <details> section goes between line 216 and line 218.

<!-- From public/css/components.css — existing patterns to follow: -->
- .section container used for content sections
- .table-wrapper wraps tables
- table styling already defined (thead, tbody, td, th)
- BEM-lite naming convention
- CSS custom properties from tokens.css (--space-*, --text-*, --color-*, --radius-*)

<!-- From template.html line 11 — claim-data meta must be updated to include new data keys: -->
<meta name="claim-data" content="schoolStart,inicio,vacacionesInicio,...">
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add collapsible section to template and update styles</name>
  <files>data/template.html, public/css/components.css, data/afirmaciones.json</files>
  <read_first>
    - data/template.html (full file — understand insertion point and existing patterns)
    - public/css/components.css (full file — understand existing table/section styles)
    - data/afirmaciones.json (understand claim structure for new data keys)
    - .planning/phases/02-datos-completos/02-CONTEXT.md (locked decisions on presentation)
  </read_first>
  <action>
    **Step A — Add collapsible `<details>` section to data/template.html:**

    Insert AFTER the main table section closing `</section>` (line 216) and BEFORE the mid ad slot (line 218). The section uses native HTML `<details>` element (per CONTEXT.md decision — no JavaScript needed):

    ```html
    <!-- Mas fechas del calendario escolar -->
    <section class="section section--secondary" aria-label="Fechas adicionales {{region}}">
      <details class="details-extra">
        <summary class="details-extra__summary">
          M&aacute;s fechas del calendario escolar &#9662;
        </summary>
        <div class="table-wrapper">
          <table>
            <thead>
              <tr>
                <th>Evento</th>
                <th>Fecha</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td>Inicio segundo semestre</td>
                <td>{{inicioSegundoSemestre}} de 2026</td>
              </tr>
              <tr>
                <td>D&iacute;a del Profesor</td>
                <td>{{diaProfesor}} de 2026</td>
              </tr>
              <tr>
                <td>Cierre actas 4&deg; Medio</td>
                <td>{{cierreActas4Medio}} de 2026</td>
              </tr>
              <tr>
                <td>Fin a&ntilde;o escolar sin JEC (40 sem.)</td>
                <td>{{finAnoSinJEC}} de 2026</td>
              </tr>
              <tr>
                <td>Fin a&ntilde;o EPJA (36 sem.)</td>
                <td>{{finAnoEPJA}} de 2026</td>
              </tr>
            </tbody>
          </table>
        </div>
        <p class="card__meta" style="margin-top:var(--space-2);">
          Fuente: <a href="https://www.mineduc.cl/resoluciones-de-calendarios-escolares-regionales-2026/" rel="noopener noreferrer" target="_blank">Resoluciones Calendarios Escolares 2026 &mdash; Mineduc</a>
        </p>
      </details>
    </section>
    ```

    **Step B — Update the claim-data meta tag (line 11 of template.html):**

    Add the 5 new data keys to the existing claim-data content attribute:
    `finAnoSinJEC,finAnoEPJA,cierreActas4Medio,diaProfesor,inicioSegundoSemestre`

    Append these after the existing keys (after `diasFiestasPatrias`).

    **Step C — Add CSS for collapsible section in public/css/components.css:**

    Add at the end of components.css:

    ```css
    /* Details-extra — collapsible secondary section */
    .section--secondary {
      margin-top: var(--space-4);
    }

    .details-extra {
      border: 1px solid var(--color-border);
      border-radius: var(--radius-lg);
      padding: 0;
      background: var(--color-bg);
    }

    .details-extra__summary {
      padding: var(--space-4) var(--space-5);
      font-size: var(--text-base);
      font-weight: 600;
      color: var(--color-text-secondary);
      cursor: pointer;
      list-style: none;
      user-select: none;
    }

    .details-extra__summary::-webkit-details-marker {
      display: none;
    }

    .details-extra[open] .details-extra__summary {
      border-bottom: 1px solid var(--color-border);
      color: var(--color-text);
    }

    .details-extra .table-wrapper {
      padding: var(--space-4) var(--space-5);
    }

    .details-extra table {
      font-size: var(--text-sm);
    }
    ```

    **Step D — Register claims in data/afirmaciones.json:**

    Add claim entries for the 5 new data keys:
    - `finAnoSinJEC`: data_source "pages.json -> finAnoSinJEC", claim_type "date", source "Resoluciones Calendarios Escolares Regionales 2026 - Mineduc"
    - `finAnoEPJA`: same pattern
    - `cierreActas4Medio`: same pattern
    - `diaProfesor`: same pattern
    - `inicioSegundoSemestre`: same pattern

    Follow the exact structure of existing claims in afirmaciones.json.

    **Step E — Regenerate pages and validate:**
    1. `node scripts/generate-pages.js` — regenerate all 16 region pages with new section
    2. `node scripts/validate.js` — must exit 0
  </action>
  <verify>
    <automated>node scripts/generate-pages.js && node scripts/validate.js && node -e "var h=require('fs').readFileSync('public/region/metropolitana/index.html','utf8'); var checks=['details-extra','inicioSegundoSemestre','diaProfesor','cierreActas4Medio','finAnoSinJEC','finAnoEPJA']; var ok=checks.every(function(c){return h.indexOf(c)!==-1}); console.log('Metropolitana has all new fields:', ok); if(!ok) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - grep "details-extra" data/template.html returns at least 2 matches
    - grep "inicioSegundoSemestre" data/template.html returns at least 1 match
    - grep "finAnoSinJEC" data/template.html returns at least 1 match
    - grep "details-extra" public/css/components.css returns at least 1 match
    - grep "section--secondary" public/css/components.css returns at least 1 match
    - public/region/metropolitana/index.html contains "details-extra"
    - public/region/metropolitana/index.html contains "16 de octubre" (dia del profesor)
    - node scripts/validate.js exits 0
    - grep "finAnoSinJEC" data/afirmaciones.json returns at least 1 match
  </acceptance_criteria>
  <done>Region pages display collapsible section with 5 additional dates, styled consistently, build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of collapsible section</name>
  <files>public/region/metropolitana/index.html, public/region/aysen/index.html</files>
  <action>
    Human visual verification of the collapsible section. No automated action — this task pauses for user review.

    What was built: Collapsible "Mas fechas del calendario escolar" section on every region page with 5 additional dates (inicio segundo semestre, dia del profesor, cierre actas 4 Medio, fin sin JEC, fin EPJA). Section is below the main table, closed by default, uses native HTML details element.

    Steps to verify:
    1. Run `npm run dev` to start local server
    2. Visit http://localhost:8788/region/metropolitana/
    3. Scroll down past the main "Calendario completo" table
    4. You should see a closed "Mas fechas del calendario escolar" section with a triangle/arrow
    5. Click it to expand — verify these dates appear:
       - Inicio segundo semestre: 6 de julio de 2026
       - Dia del Profesor: 16 de octubre de 2026
       - Cierre actas 4 Medio: 20 de noviembre de 2026
       - Fin ano escolar sin JEC: 18 de diciembre de 2026
       - Fin ano EPJA: 20 de noviembre de 2026
    6. Verify styling is consistent (same border, same font, same spacing as main table)
    7. Visit http://localhost:8788/region/aysen/ — verify dates are region-specific (Aysen has different winter recess)
    8. Check mobile view (resize browser to ~375px) — table should not overflow
  </action>
  <verify>User confirms visual appearance is correct by typing "approved"</verify>
  <done>User has visually verified the collapsible section on at least 2 region pages (Metropolitana + Aysen) and confirmed styling consistency</done>
</task>

</tasks>

<verification>
1. All 16 region pages have the collapsible `<details>` section
2. Section is closed by default, expands on click
3. 5 additional dates are visible when expanded
4. Styling matches existing table design (borders, fonts, spacing)
5. `npm run build` passes (validate.js exits 0)
6. claim-data meta tag includes the 5 new data keys
7. afirmaciones.json has claims for all 5 new data keys
</verification>

<success_criteria>
- Collapsible section visible on all 16 region pages
- Section closed by default, labeled "Mas fechas del calendario escolar"
- 5 additional dates shown in a simple 2-column table (Evento, Fecha)
- Visual style consistent with existing page design
- Mobile-responsive (no horizontal overflow)
- Build pipeline passes (generate + validate)
</success_criteria>

<output>
After completion, create `.planning/phases/02-datos-completos/02-02-SUMMARY.md`
</output>
