---
phase: 03-region-selector-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/css/components.css
  - public/index.html
autonomous: true
requirements:
  - MAP-01
  - MAP-03
  - MAP-04
  - PANEL-01
  - PANEL-02
  - PANEL-03
  - RESP-01

must_haves:
  truths:
    - "User sees 16 region bars ordered north-to-south with color dots by group"
    - "User sees a legend explaining 4 group colors"
    - "User sees a placeholder message before selecting any region"
    - "Desktop layout shows region list (220px) on left and data panel on right"
    - "Key-facts section is removed from the page"
    - "Chips and hidden select are removed from the page"
  artifacts:
    - path: "public/css/components.css"
      provides: "Map layout, region bar, data panel, mini-facts, legend, grupo-badge styles"
      contains: ".map-layout"
    - path: "public/index.html"
      provides: "Map layout HTML with 16 region bars, data panel, legend"
      contains: "map-layout"
  key_links:
    - from: "public/index.html"
      to: "public/css/components.css"
      via: "CSS class references"
      pattern: "map-layout|region-bar|data-panel|mini-fact"
---

<objective>
Replace the homepage chips-based region selector with Mock C's split layout: a color-coded region list on the left and a data panel on the right (desktop). Add all CSS styles and HTML structure.

Purpose: Transform the region selection from a flat chip grid into an interactive north-to-south list with visual group indicators, matching the approved Mock C design.
Output: Updated index.html with new HTML structure, updated components.css with all new styles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-region-selector-panel/03-CONTEXT.md
@mocks/mock-c-mapa.html
@public/css/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add map layout CSS styles to components.css</name>
  <files>public/css/components.css</files>
  <read_first>
    - public/css/components.css
    - public/css/tokens.css
    - mocks/mock-c-mapa.html (lines 38-121 for CSS reference)
  </read_first>
  <action>
Append new component styles to the END of public/css/components.css (after the existing `.details-extra` block, around line 643). Use the project's existing CSS custom properties from tokens.css (NOT the mock's local variables). Follow BEM-lite naming.

Add these style blocks in order:

1. **Map Layout container** (`.map-layout`):
```css
/* ============================================
   Map Layout — region list + data panel
   ============================================ */
.map-layout {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--space-4);
  margin: var(--space-6) 0;
}
@media (min-width: 650px) {
  .map-layout {
    grid-template-columns: 220px 1fr;
  }
}
```

2. **Map Panel** (`.map-panel`) — left sidebar with region list:
```css
.map-panel {
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-4);
  box-shadow: var(--shadow-sm);
}
.map-panel__title {
  font-size: var(--text-sm);
  color: var(--color-text-secondary);
  margin-bottom: var(--space-3);
  text-align: center;
  font-weight: 600;
}
```

3. **Region Bar** (`.region-bar`) — individual clickable region row:
```css
.region-bar {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  padding: 6px 8px;
  border-radius: var(--radius-sm);
  cursor: pointer;
  font-size: var(--text-xs);
  transition: background var(--transition-fast), color var(--transition-fast);
  border: 1.5px solid transparent;
  background: none;
  width: 100%;
  text-align: left;
  font-family: var(--font-sans);
  color: var(--color-text);
  line-height: 1.3;
}
.region-bar:hover {
  background: var(--color-primary-light);
}
.region-bar.active {
  background: var(--color-primary);
  color: #fff;
  border-color: var(--color-primary);
}
.region-bar__dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--color-primary);
  flex-shrink: 0;
}
.region-bar.active .region-bar__dot {
  background: #fff;
}
/* Group colors for dots */
.region-bar[data-grupo="NORTE"] .region-bar__dot { background: var(--color-warning); }
.region-bar[data-grupo="SUR"] .region-bar__dot { background: var(--color-accent); }
.region-bar[data-grupo="SUR-PARCIAL"] .region-bar__dot { background: var(--color-success); }
/* Active overrides for group colors */
.region-bar.active[data-grupo="NORTE"] .region-bar__dot,
.region-bar.active[data-grupo="SUR"] .region-bar__dot,
.region-bar.active[data-grupo="SUR-PARCIAL"] .region-bar__dot { background: #fff; }
```

4. **Data Panel** (`.data-panel`) — right side content area:
```css
.data-panel {
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: var(--space-6);
  box-shadow: var(--shadow-sm);
  min-height: 300px;
}
.data-panel__title {
  font-size: var(--text-xl);
  font-weight: 700;
  margin-bottom: var(--space-2);
}
.data-panel__grupo {
  display: inline-block;
  font-size: 0.65rem;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  padding: 2px 10px;
  border-radius: var(--radius-full);
  background: var(--color-primary-light);
  color: var(--color-primary);
  margin-bottom: var(--space-4);
}
```

5. **Mini Facts grid** (`.mini-facts`) — 2x2 key dates in panel:
```css
.mini-facts {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--space-3);
  margin-bottom: var(--space-6);
}
.mini-fact {
  padding: var(--space-3);
  background: var(--color-surface);
  border-radius: var(--radius);
}
.mini-fact__label {
  font-size: 0.65rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--color-text-secondary);
  margin-bottom: 2px;
}
.mini-fact__value {
  font-size: var(--text-base);
  font-weight: 700;
  color: var(--color-text);
}
```

6. **Legend** (`.map-legend`):
```css
.map-legend {
  display: flex;
  gap: var(--space-4);
  justify-content: center;
  margin: var(--space-3) 0;
  flex-wrap: wrap;
}
.map-legend__item {
  font-size: var(--text-xs);
  color: var(--color-text-secondary);
  display: flex;
  align-items: center;
  gap: 4px;
}
.map-legend__dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  display: inline-block;
}
```

7. **Placeholder state** (`.data-panel__placeholder`):
```css
.data-panel__placeholder {
  text-align: center;
  padding: 4rem 0;
  color: var(--color-text-secondary);
}
.data-panel__placeholder-icon {
  font-size: 2rem;
  margin-bottom: var(--space-2);
}
```

8. **Region data visibility toggle**:
```css
#region-data {
  display: none;
}
#region-data.active {
  display: block;
}
```

9. **Dark mode overrides** for new components:
```css
@media (prefers-color-scheme: dark) {
  :root:not([data-theme="light"]) .map-panel,
  :root:not([data-theme="light"]) .data-panel {
    background: var(--color-surface);
  }
  :root:not([data-theme="light"]) .mini-fact {
    background: var(--color-surface-alt);
  }
  :root:not([data-theme="light"]) .region-bar:hover {
    background: var(--color-surface-alt);
  }
}
[data-theme="dark"] .map-panel,
[data-theme="dark"] .data-panel {
  background: var(--color-surface);
}
[data-theme="dark"] .mini-fact {
  background: var(--color-surface-alt);
}
[data-theme="dark"] .region-bar:hover {
  background: var(--color-surface-alt);
}
```

Do NOT modify any existing styles. Only append at the end.
  </action>
  <verify>
    <automated>grep -c "map-layout\|map-panel\|region-bar\|data-panel\|mini-fact\|map-legend\|data-panel__placeholder" public/css/components.css | grep -v "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - grep "\.map-layout" public/css/components.css returns at least 1 match
    - grep "grid-template-columns: 220px 1fr" public/css/components.css returns 1 match
    - grep "\.region-bar\b" public/css/components.css returns at least 5 matches (base + hover + active + dot + group)
    - grep "\.data-panel\b" public/css/components.css returns at least 2 matches
    - grep "\.mini-fact\b" public/css/components.css returns at least 2 matches
    - grep "\.map-legend\b" public/css/components.css returns at least 1 match
    - grep "data-grupo.*NORTE" public/css/components.css returns at least 1 match
    - grep "data-grupo.*SUR-PARCIAL" public/css/components.css returns at least 1 match
    - No existing styles above the new block were modified
  </acceptance_criteria>
  <done>All map layout CSS classes exist in components.css using project tokens. Dark mode variants included. Existing styles untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Replace homepage HTML with map layout structure</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (full file)
    - mocks/mock-c-mapa.html (lines 140-231 for HTML structure reference)
    - public/js/regions-data.js (for slug names and data-grupo attributes)
    - .planning/phases/03-region-selector-panel/03-CONTEXT.md
  </read_first>
  <action>
Make these changes to public/index.html per CONTEXT.md decisions:

**A) REMOVE the key-facts section** (lines 176-200 approximately):
Delete the entire `<section class="key-facts" ...>` block including its closing tag and the Mineduc source paragraph. This section showed static dates that will now be shown in the data panel per user decision.

**B) REPLACE the chips section** (lines 231-297 approximately):
Delete from `<!-- Selector de region por chips -->` through `</section>` (the entire section containing the fieldset.region-chips, the hidden select#region-select, and the div#region-calendar).

Replace with this structure (using semantic HTML, accessible, following Mock C layout):

```html
      <!-- Selector de region + panel de datos -->
      <section class="section" aria-label="Consulta por regi&oacute;n">
        <h2>&iquest;De qu&eacute; regi&oacute;n eres?</h2>

        <div class="map-legend" role="img" aria-label="Leyenda de grupos regionales">
          <span class="map-legend__item"><span class="map-legend__dot" style="background:var(--color-primary)"></span> Est&aacute;ndar (11)</span>
          <span class="map-legend__item"><span class="map-legend__dot" style="background:var(--color-warning)"></span> Norte (2)</span>
          <span class="map-legend__item"><span class="map-legend__dot" style="background:var(--color-success)"></span> Sur-Parcial (1)</span>
          <span class="map-legend__item"><span class="map-legend__dot" style="background:var(--color-accent)"></span> Sur (2)</span>
        </div>

        <div class="map-layout">
          <!-- Lista de regiones -->
          <div class="map-panel" role="listbox" aria-label="Regiones de Chile">
            <p class="map-panel__title">Regiones de Chile</p>
            <div class="chile-map">
              <button type="button" class="region-bar" role="option" aria-selected="false" data-grupo="NORTE" data-slug="arica-y-parinacota"><span class="region-bar__dot"></span> Arica y Parinacota</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-grupo="NORTE" data-slug="tarapaca"><span class="region-bar__dot"></span> Tarapac&aacute;</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-slug="antofagasta"><span class="region-bar__dot"></span> Antofagasta</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-slug="atacama"><span class="region-bar__dot"></span> Atacama</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-slug="coquimbo"><span class="region-bar__dot"></span> Coquimbo</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-slug="valparaiso"><span class="region-bar__dot"></span> Valpara&iacute;so</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-slug="metropolitana"><span class="region-bar__dot"></span> Metropolitana</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-slug="ohiggins"><span class="region-bar__dot"></span> O&rsquo;Higgins</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-slug="maule"><span class="region-bar__dot"></span> Maule</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-slug="nuble"><span class="region-bar__dot"></span> &Ntilde;uble</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-slug="biobio"><span class="region-bar__dot"></span> Biob&iacute;o</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-slug="araucania"><span class="region-bar__dot"></span> La Araucan&iacute;a</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-slug="los-rios"><span class="region-bar__dot"></span> Los R&iacute;os</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-grupo="SUR-PARCIAL" data-slug="los-lagos"><span class="region-bar__dot"></span> Los Lagos</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-grupo="SUR" data-slug="aysen"><span class="region-bar__dot"></span> Ays&eacute;n</button>
              <button type="button" class="region-bar" role="option" aria-selected="false" data-grupo="SUR" data-slug="magallanes"><span class="region-bar__dot"></span> Magallanes</button>
            </div>
          </div>

          <!-- Panel de datos -->
          <div class="data-panel">
            <div id="placeholder-data" class="data-panel__placeholder">
              <p class="data-panel__placeholder-icon" aria-hidden="true">&#x1F449;</p>
              <p>Selecciona una regi&oacute;n para ver su calendario</p>
            </div>

            <div id="region-data">
              <h3 class="data-panel__title" id="r-name"></h3>
              <span class="data-panel__grupo" id="r-grupo"></span>

              <div class="mini-facts">
                <div class="mini-fact">
                  <p class="mini-fact__label">Inicio clases</p>
                  <p class="mini-fact__value" id="r-inicio"></p>
                </div>
                <div class="mini-fact">
                  <p class="mini-fact__label">Vacaciones invierno</p>
                  <p class="mini-fact__value" id="r-vac"></p>
                </div>
                <div class="mini-fact">
                  <p class="mini-fact__label">Fiestas Patrias</p>
                  <p class="mini-fact__value" id="r-fp"></p>
                </div>
                <div class="mini-fact">
                  <p class="mini-fact__label">Fin de a&ntilde;o</p>
                  <p class="mini-fact__value" id="r-fin"></p>
                </div>
              </div>

              <details open>
                <summary>Datos adicionales</summary>
                <table>
                  <tbody>
                    <tr><td>Inicio 2do semestre</td><td id="r-seg"></td></tr>
                    <tr><td>D&iacute;a del Profesor</td><td id="r-prof"></td></tr>
                    <tr><td>Cierre actas 4&deg; Medio</td><td id="r-actas"></td></tr>
                    <tr><td>Fin sin JEC (40 sem.)</td><td id="r-sinjec"></td></tr>
                    <tr><td>Fin EPJA (36 sem.)</td><td id="r-epja"></td></tr>
                  </tbody>
                </table>
              </details>

              <div style="margin-top:var(--space-4); text-align:center;">
                <a href="#" class="btn btn--primary" id="link-pagina">Ver p&aacute;gina completa &rarr;</a>
              </div>
            </div>
          </div>
        </div>
      </section>
```

**Key details:**
- Each region bar is a `<button>` (not a div) for keyboard accessibility.
- ESTANDAR regions have NO `data-grupo` attribute (they use the default primary dot color from CSS).
- NORTE: arica-y-parinacota, tarapaca
- SUR-PARCIAL: los-lagos
- SUR: aysen, magallanes
- Slugs match exactly the keys in REGIONS_DATA (e.g., `arica-y-parinacota` not `arica`).
- Data panel IDs: r-name, r-grupo, r-inicio, r-vac, r-fp, r-fin, r-seg, r-prof, r-actas, r-sinjec, r-epja, link-pagina.
- The `<details open>` for additional data matches CONTEXT.md decision ("open by default").
- All 16 regions ordered north to south.
- Keep the school-stats section above and feriados section below untouched.
- Keep the ad slot between school-stats and the new section untouched.
  </action>
  <verify>
    <automated>node -e "var h=require('fs').readFileSync('public/index.html','utf8'); var checks=[h.includes('map-layout'),h.includes('map-panel'),h.includes('region-bar'),h.includes('data-panel'),h.includes('map-legend'),h.includes('placeholder-data'),h.includes('region-data'),h.includes('link-pagina'),(h.match(/region-bar/g)||[]).length>=18,!h.includes('region-chips'),!h.includes('region-select'),!h.includes('key-facts')]; console.log(checks.every(Boolean)?'PASS':'FAIL: '+JSON.stringify(checks));"</automated>
  </verify>
  <acceptance_criteria>
    - grep "map-layout" public/index.html returns at least 1 match
    - grep "region-bar.*data-slug" public/index.html returns exactly 16 matches (one per region)
    - grep "data-grupo=\"NORTE\"" public/index.html returns exactly 2 matches
    - grep "data-grupo=\"SUR\"" public/index.html returns exactly 2 matches
    - grep "data-grupo=\"SUR-PARCIAL\"" public/index.html returns exactly 1 match
    - grep "region-chips" public/index.html returns 0 matches (chips removed)
    - grep "region-select" public/index.html returns 0 matches (hidden select removed)
    - grep "key-facts" public/index.html returns 0 matches (static cards removed)
    - grep "school-stats" public/index.html returns at least 1 match (preserved)
    - grep "placeholder-data" public/index.html returns 1 match
    - grep "id=\"r-name\"" public/index.html returns 1 match
    - grep "id=\"r-vac\"" public/index.html returns 1 match
    - grep "id=\"link-pagina\"" public/index.html returns 1 match
    - grep "map-legend" public/index.html returns at least 2 matches
  </acceptance_criteria>
  <done>index.html has the new map layout HTML with 16 region bars (north to south), data panel with placeholder, legend, and all data IDs. Old chips, hidden select, and key-facts sections are gone. School-stats and feriados sections unchanged.</done>
</task>

</tasks>

<verification>
- Open index.html in browser at >650px width: region list appears on left (220px), data panel on right
- All 16 regions visible in list, ordered Arica to Magallanes
- Color dots: purple (default), orange (NORTE), green (SUR-PARCIAL), blue (SUR)
- Legend visible above the layout showing 4 groups
- Placeholder message "Selecciona una region" visible in panel
- School-stats bar still present above
- Feriados table still present below
- No JS errors in console (region bars are not yet wired — that's Plan 02)
</verification>

<success_criteria>
HTML structure matches Mock C layout. CSS styles use project tokens. 16 regions ordered north-to-south with correct group attributes. Old chips/select/key-facts removed. Layout is 220px | 1fr on desktop.
</success_criteria>

<output>
After completion, create `.planning/phases/03-region-selector-panel/03-01-SUMMARY.md`
</output>
