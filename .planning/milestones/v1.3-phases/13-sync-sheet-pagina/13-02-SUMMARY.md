---
phase: 13-sync-sheet-pagina
plan: "02"
subsystem: frontend-data-pipeline
tags: [claims, tooltips, pipeline, sheet-sync, tech-debt]
dependency_graph:
  requires: ["13-01"]
  provides: ["claims-data.js", "claims-tooltips.js", "dynamic-tooltip-rendering"]
  affects: ["public/index.html", "scripts/generate-pages.js", "scripts/generate-verificacion.js"]
tech_stack:
  added: []
  patterns: ["IIFE-module", "data-attribute-driven-rendering", "generated-js-from-json"]
key_files:
  created:
    - public/js/claims-data.js
    - public/js/claims-tooltips.js
  modified:
    - scripts/generate-pages.js
    - scripts/generate-verificacion.js
    - public/index.html
key_decisions:
  - "claims-tooltips.js generated by generate-pages.js (not manually maintained) — keeps tooltip logic in sync with data model"
  - "generate-verificacion.js reads claims.json primary with afirmaciones.json fallback — backward compatible"
  - "data-claim-id attribute pattern on bcn-badge-wrap — clean separation of HTML structure from data"
patterns-established:
  - "data-attribute-driven rendering: HTML carries only data-claim-id, JS populates text at runtime from CLAIMS_DATA"
  - "Generated JS files (claims-data.js, claims-tooltips.js) never edited manually — always from npm run generate"
requirements-completed: [SYNC-02, SYNC-04]
metrics:
  duration: "8 min"
  completed: "2026-03-25"
  tasks_completed: 1
  tasks_total: 2
  files_modified: 5
---

# Phase 13 Plan 02: Dynamic Tooltips from Claims.json — Summary

**Eliminates tech debt INT-01 by generating CLAIMS_DATA JS from claims.json and replacing 7 hardcoded BCN tooltip texts with data-claim-id driven rendering via generated claims-tooltips.js IIFE.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-25T16:44:00Z
- **Completed:** 2026-03-25
- **Tasks:** 1 of 2 (Task 2 was a human-verify checkpoint, deferred by user)
- **Files modified:** 5

## Accomplishments

- generate-pages.js now produces `public/js/claims-data.js` (50 claims as `window.CLAIMS_DATA`) and `public/js/claims-tooltips.js` (IIFE that populates tooltips on DOMContentLoaded)
- All 7 hardcoded BCN tooltip texts removed from index.html; replaced with empty `.bcn-tooltip` spans and `data-claim-id` attributes
- generate-verificacion.js updated to read `claims.json` as primary source with `afirmaciones.json` fallback — backward compatible
- `npm run build` passes end-to-end with new generated files

## Task Commits

Each task was committed atomically:

1. **Task 1: Generate claims-data.js + dynamic tooltips** - `43d2979` (feat)
2. **Task 2: End-to-end pipeline verification** - checkpoint deferred by user (human-verify)

**Plan metadata:** `c05f0c0` (partial) — final: see docs commit below

## Files Created/Modified

- `scripts/generate-pages.js` — Added two generation blocks: claims-data.js (50 claims map) and claims-tooltips.js (IIFE module)
- `scripts/generate-verificacion.js` — Reads claims.json first, falls back to afirmaciones.json; variable aliased for backward compat
- `public/index.html` — 7 bcn-badge-wrap spans get data-claim-id; 7 bcn-tooltip spans emptied; two new script tags added
- `public/js/claims-data.js` (new, generated) — `window.CLAIMS_DATA` keyed by claim.id with source_reference, extracto_verbatim, fuente_url, etc.
- `public/js/claims-tooltips.js` (new, generated) — IIFE that queries `[data-claim-id]` and populates tooltip text from CLAIMS_DATA

## Decisions Made

- `claims-tooltips.js` is generated by `generate-pages.js`, not manually maintained — tooltip logic stays in sync with data model automatically
- `generate-verificacion.js` uses claims.json as primary source with afirmaciones.json fallback — ensures backward compatibility while migrating
- `data-claim-id` attribute on the wrapper `.bcn-badge-wrap` element (not the tooltip span) — cleaner HTML, single attribute per badge

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. All 7 tooltip spans are populated at runtime by claims-tooltips.js from CLAIMS_DATA. The claims.json data contains extracto_verbatim for the BCN feriado claims used in these tooltips.

## Issues Encountered

Task 2 was a `checkpoint:human-verify` gate requiring browser verification of tooltip rendering. The user deferred this validation checkpoint. The pipeline (generate + build) was verified programmatically and passes.

## User Setup Required

None — no external service configuration required for this plan.

## Next Phase Readiness

- Phase 13 complete: full Sheet-to-page pipeline operational (sync → claims.json → generate → HTML)
- Phase 14 (Notificaciones Telegram) can now proceed — depends on sync pipeline context established here
- To validate tooltips visually: run `npm run generate` then open `public/index.html` in browser, hover over feriado rows

## Self-Check: PASSED

- `public/js/claims-data.js` — FOUND (generated)
- `public/js/claims-tooltips.js` — FOUND (generated)
- Commit 43d2979 — FOUND
- 7 data-claim-id attributes in index.html — CONFIRMED
- generate-verificacion.js reads claims.json — CONFIRMED
- npm run build — PASS

---
*Phase: 13-sync-sheet-pagina*
*Completed: 2026-03-25*
