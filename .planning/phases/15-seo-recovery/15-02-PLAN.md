---
phase: 15-seo-recovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
  - public/js/app.js
  - public/css/components.css
autonomous: true
requirements:
  - SEO-B3
  - SEO-B4
  - SEO-B5
  - SEO-B6

must_haves:
  truths:
    - "public/index.html <title> contiene 'Fechas Oficiales por Región'"
    - "public/index.html contiene <h2 class=\"region-picker__h2\">"
    - "public/index.html contiene <section class=\"region-index\"> con 16 <li><a href=\"/region/..."
    - "public/index.html contiene <section class=\"home-stats\"> con stat-week, stat-days-winter, stat-feriados-left"
    - "public/js/app.js contiene función initHomeStats()"
    - "public/index.html <script defer src=\"/js/app.js?v=4\">"
    - "public/index.html <link rel=\"stylesheet\" href=\"/css/components.css?v=5\">"
    - "Schema dateModified en public/index.html es 2026-04-23"
  artifacts:
    - path: "public/index.html"
      provides: "Home con title revertido, H2 region-intent, link graph 16 anchors, stats bar, dateModified actualizado"
      contains: "Fechas Oficiales por Región"
    - path: "public/js/app.js"
      provides: "initHomeStats() con semana escolar, días a vacaciones invierno, feriados restantes"
      contains: "initHomeStats"
    - path: "public/css/components.css"
      provides: "Estilos para .region-index y .home-stats"
      contains: "region-index"
  key_links:
    - from: "public/js/app.js"
      to: "public/js/calendar-config.js"
      via: "lee CAL (CALENDAR_CONFIG) para calcular stats"
      pattern: "CAL"
    - from: "public/index.html"
      to: "public/region/*/index.html"
      via: "link graph anchor text crawlable"
      pattern: "href=\"/region/"
---

<objective>
Recuperar señales de contenido en la home: revertir title al patrón ranker histórico, restaurar link graph a 16 regiones, agregar stats bar de contenido único time-sensitive (señal destination-source), y refrescar fechas de modificación.

Contexto: el refactor ce2835f del 2026-04-17 eliminó el title ranker, el H2 region-intent, los 16 anchor links (reemplazados por <select>), y el contenido estadístico único. El March 2026 Core Update busca "destination sources" — este plan inyecta señales diferenciadores que un aggregator puro no puede tener.

Output: home.html con title revertido, H2 + 16 anchors crawlables, stats bar dinámica (semana escolar actual, días a vacaciones invierno, feriados restantes en período escolar), y dateModified correcto en todos los schemas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revertir title, meta description, og:title, og:description, twitter:title, twitter:description (home)</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (líneas 1-40 — ver title, meta description, og tags, twitter tags actuales)
  </read_first>
  <action>
En `public/index.html`:

**Cambio 1 — `<title>`**: Reemplazar el title actual por:
```html
<title>Calendario Escolar 2026 Chile &mdash; Fechas Oficiales por Regi&oacute;n</title>
```

**Cambio 2 — `<meta name="description">`**: Reemplazar el content actual por:
```
content="Calendario escolar 2026 Chile: inicio 4 de marzo, vacaciones de invierno desde el 22 de junio, fin de a&ntilde;o 4 de diciembre. Fechas oficiales Mineduc para las 16 regiones. Actualizado abril 2026."
```

**Cambio 3 — `<meta property="og:title">`**: Reemplazar content por:
```
content="Calendario Escolar 2026 Chile &mdash; Fechas Oficiales por Regi&oacute;n"
```

**Cambio 4 — `<meta property="og:description">`**: Reemplazar content por:
```
content="Inicio 4 de marzo, vacaciones desde el 22 de junio, fin 4 de diciembre. Fechas Mineduc para las 16 regiones de Chile."
```

**Cambio 5 — `<meta name="twitter:title">`**: Reemplazar content por:
```
content="Calendario Escolar 2026 Chile &mdash; Fechas Oficiales por Regi&oacute;n"
```

**Cambio 6 — `<meta name="twitter:description">`**: Reemplazar content por:
```
content="Inicio 4 de marzo, vacaciones desde el 22 de junio, fin 4 de diciembre. 16 regiones. Fuente Mineduc."
```
  </action>
  <verify>
    <automated>grep -A1 "<title>" public/index.html | head -2</automated>
  </verify>
  <acceptance_criteria>
    - grep 'title' public/index.html (primer match) contiene "Fechas Oficiales por Región"
    - grep 'og:title' public/index.html contiene "Fechas Oficiales por Región"
    - grep 'twitter:title' public/index.html contiene "Fechas Oficiales por Región"
    - grep 'name="description"' public/index.html contiene "16 regiones"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: H2 semántico region-intent + sección region-index con 16 anchors</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (buscar la sección .region-picker — contiene el label "¿Cuándo empiezan las clases en tu región?" y el <select id="region-select">)
    - public/index.html (buscar .more-cards o sección posterior al region-picker para ubicar dónde insertar region-index)
  </read_first>
  <action>
En `public/index.html`:

**Cambio 1 — agregar H2 dentro de `.region-picker`**: Insertar inmediatamente después de `<section class="region-picker"...>` y ANTES del `<label class="region-picker__label"`:
```html
  <h2 class="region-picker__h2">&iquest;De qu&eacute; regi&oacute;n eres?</h2>
```

**Cambio 2 — agregar sección region-index**: Insertar DESPUÉS del cierre de `</section>` de `.region-picker` y ANTES de lo que venga después (`.more-cards` u otro elemento). Insertar este bloque completo:
```html
<!-- Link graph: 16 regiones con anchor text crawlable -->
<section class="region-index" aria-label="&Iacute;ndice de regiones">
  <h2 class="region-index__heading">Calendario escolar por regi&oacute;n</h2>
  <ul class="region-index__list">
    <li><a href="/region/arica-y-parinacota/">Arica y Parinacota</a></li>
    <li><a href="/region/tarapaca/">Tarapac&aacute;</a></li>
    <li><a href="/region/antofagasta/">Antofagasta</a></li>
    <li><a href="/region/atacama/">Atacama</a></li>
    <li><a href="/region/coquimbo/">Coquimbo</a></li>
    <li><a href="/region/valparaiso/">Valpara&iacute;so</a></li>
    <li><a href="/region/metropolitana/">Metropolitana</a></li>
    <li><a href="/region/ohiggins/">O&rsquo;Higgins</a></li>
    <li><a href="/region/maule/">Maule</a></li>
    <li><a href="/region/nuble/">&Ntilde;uble</a></li>
    <li><a href="/region/biobio/">Biob&iacute;o</a></li>
    <li><a href="/region/araucania/">La Araucan&iacute;a</a></li>
    <li><a href="/region/los-rios/">Los R&iacute;os</a></li>
    <li><a href="/region/los-lagos/">Los Lagos</a></li>
    <li><a href="/region/aysen/">Ays&eacute;n</a></li>
    <li><a href="/region/magallanes/">Magallanes</a></li>
  </ul>
</section>
```

Verificar que el `<select id="region-select">` NO fue eliminado — app.js depende de él para initRegionPicker.
  </action>
  <verify>
    <automated>grep -c 'href="/region/' public/index.html</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'href="/region/' public/index.html retorna ≥16
    - grep 'region-picker__h2' public/index.html retorna 1 línea
    - grep 'region-index' public/index.html retorna al menos 1 línea
    - grep 'id="region-select"' public/index.html retorna 1 línea (select no eliminado)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Stats bar home-stats (semana escolar, días a vacaciones, feriados restantes)</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (buscar .feriado-card y .region-picker para ubicar el punto de inserción — la stats bar va DESPUÉS de .feriado-card y ANTES de .region-picker)
  </read_first>
  <action>
En `public/index.html`, insertar el siguiente bloque DESPUÉS del cierre `</section>` de `.feriado-card` y ANTES del `<section class="region-picker"`:

```html
<!-- Stats en tiempo real (contenido único destination-source) -->
<section class="home-stats" aria-label="Estado actual del a&ntilde;o escolar 2026">
  <div class="home-stats__item">
    <p class="home-stats__label">Semana escolar</p>
    <p class="home-stats__value" id="stat-week">&mdash;</p>
    <p class="home-stats__note">de 38 (JEC)</p>
  </div>
  <div class="home-stats__item">
    <p class="home-stats__label">Faltan</p>
    <p class="home-stats__value" id="stat-days-winter">&mdash;</p>
    <p class="home-stats__note">d&iacute;as para vacaciones invierno</p>
  </div>
  <div class="home-stats__item">
    <p class="home-stats__label">Feriados restantes</p>
    <p class="home-stats__value" id="stat-feriados-left">&mdash;</p>
    <p class="home-stats__note">en per&iacute;odo escolar</p>
  </div>
</section>
```
  </action>
  <verify>
    <automated>grep -c 'home-stats' public/index.html</automated>
  </verify>
  <acceptance_criteria>
    - grep 'class="home-stats"' public/index.html retorna 1 línea
    - grep 'id="stat-week"' public/index.html retorna 1 línea
    - grep 'id="stat-days-winter"' public/index.html retorna 1 línea
    - grep 'id="stat-feriados-left"' public/index.html retorna 1 línea
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: initHomeStats() en app.js + llamada desde init()</name>
  <files>public/js/app.js</files>
  <read_first>
    - public/js/app.js (leer completo — ver estructura IIFE, función init(), initFeriadoCard(), initRegionPicker(), variables CAL, today(), parseDate())
  </read_first>
  <action>
En `public/js/app.js`, agregar la función `initHomeStats()` ANTES de la función `init()`. Seguir el mismo patrón de `var`, `function` declarations del archivo existente (no usar const/let/arrow functions):

```javascript
function initHomeStats() {
  var weekEl = document.getElementById('stat-week');
  var daysEl = document.getElementById('stat-days-winter');
  var feriadosEl = document.getElementById('stat-feriados-left');
  if (!weekEl || !daysEl || !feriadosEl || !CAL) return;

  var now = today();
  var schoolStart = parseDate(CAL.schoolStart);
  var winterStart = parseDate(CAL.winterStart);

  // Semana del año escolar (1-38 aprox)
  if (now >= schoolStart) {
    var diffMs = now - schoolStart;
    var week = Math.floor(diffMs / (7 * 86400000)) + 1;
    weekEl.textContent = week > 0 && week <= 45 ? week : '—';
  } else {
    weekEl.textContent = 'Pre';
  }

  // Días para vacaciones invierno
  if (now < winterStart) {
    var diffDays = Math.ceil((winterStart - now) / 86400000);
    daysEl.textContent = diffDays;
  } else {
    var winterEnd = parseDate(CAL.winterEnd);
    if (now <= winterEnd) {
      daysEl.textContent = '0';
      var daysElNote = daysEl.nextElementSibling;
      if (daysElNote) daysElNote.textContent = 'ya estamos de vacaciones';
    } else {
      daysEl.textContent = '—';
    }
  }

  // Feriados restantes en período escolar
  var source = CAL.feriadosCompletos || [];
  var count = 0;
  for (var i = 0; i < source.length; i++) {
    var f = source[i];
    if (f.contexto && f.contexto !== 'en-clases') continue;
    var d = parseDate(f.date);
    if (d >= now) count++;
  }
  feriadosEl.textContent = count;
}
```

Luego, en la función `init()`, agregar la llamada a `initHomeStats()` JUNTO a `initFeriadoCard()`, dentro del bloque `if (CAL)`:

```javascript
function init() {
  if (CAL) {
    initFeriadoCard();
    initHomeStats();
  } else {
    console.warn('[app.js] CALENDAR_CONFIG no disponible — ejecutar: npm run generate');
  }
  initRegionPicker();
}
```

NO modificar ninguna otra función ni la estructura del IIFE.
  </action>
  <verify>
    <automated>grep -n "initHomeStats" public/js/app.js</automated>
  </verify>
  <acceptance_criteria>
    - grep 'function initHomeStats' public/js/app.js retorna 1 línea
    - grep 'initHomeStats()' public/js/app.js retorna ≥2 líneas (definición + llamada en init)
    - grep 'stat-week' public/js/app.js retorna 1 línea
    - grep 'stat-days-winter' public/js/app.js retorna 1 línea
    - grep 'stat-feriados-left' public/js/app.js retorna 1 línea
    - grep 'feriadosCompletos' public/js/app.js retorna 1 línea
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: CSS para .region-picker__h2, .region-index, .home-stats en components.css</name>
  <files>public/css/components.css</files>
  <read_first>
    - public/css/components.css (últimas 30 líneas — ver fin del archivo para hacer append correcto)
    - public/css/tokens.css (ver si existe --color-surface-alt; si no, usar var(--color-surface))
  </read_first>
  <action>
En `public/css/components.css`, AGREGAR al final del archivo (sin modificar nada existente) los siguientes bloques CSS:

```css
/* ---------- Region picker H2 ---------- */
.region-picker__h2 {
  font-size: var(--text-lg);
  font-weight: 700;
  color: var(--color-text);
  margin: 0 0 var(--space-3) 0;
  text-align: center;
}

/* ---------- Region index (link graph) ---------- */
.region-index {
  margin-top: var(--space-5);
  padding: var(--space-4);
}
.region-index__heading {
  font-size: var(--text-base);
  font-weight: 600;
  color: var(--color-text-muted);
  margin: 0 0 var(--space-3) 0;
  text-align: center;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}
.region-index__list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--space-1) var(--space-3);
}
@media (min-width: 600px) {
  .region-index__list {
    grid-template-columns: repeat(4, 1fr);
  }
}
.region-index__list a {
  display: block;
  padding: var(--space-2) 0;
  color: var(--color-primary);
  text-decoration: none;
  font-size: var(--text-sm);
  border-bottom: 1px solid transparent;
}
.region-index__list a:hover,
.region-index__list a:focus {
  border-bottom-color: var(--color-primary);
}

/* ---------- Home stats (destination-source signal) ---------- */
.home-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--space-2);
  margin: var(--space-4) 0;
  padding: var(--space-4);
  background: var(--color-surface);
  border-radius: var(--radius-md);
  border: 1px solid var(--color-border);
}
.home-stats__item {
  text-align: center;
}
.home-stats__label {
  font-size: var(--text-xs);
  color: var(--color-text-muted);
  margin: 0 0 var(--space-1) 0;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}
.home-stats__value {
  font-size: var(--text-xl);
  font-weight: 700;
  color: var(--color-primary);
  margin: 0;
  line-height: 1.1;
}
.home-stats__note {
  font-size: var(--text-xs);
  color: var(--color-text-muted);
  margin: var(--space-1) 0 0 0;
}
@media (max-width: 480px) {
  .home-stats__value { font-size: var(--text-lg); }
}
```

Nota: usar `var(--color-surface)` (no `--color-surface-alt`) para garantizar compatibilidad — si `--color-surface-alt` no existe en tokens.css, causaría fallback silencioso.
  </action>
  <verify>
    <automated>grep -c 'region-index\|home-stats' public/css/components.css</automated>
  </verify>
  <acceptance_criteria>
    - grep '.region-index' public/css/components.css retorna ≥1 línea
    - grep '.home-stats' public/css/components.css retorna ≥1 línea
    - grep '.region-picker__h2' public/css/components.css retorna ≥1 línea
    - grep 'grid-template-columns: repeat(3' public/css/components.css retorna 1 línea (home-stats grid)
    - grep 'grid-template-columns: repeat(4' public/css/components.css retorna 1 línea (region-index responsive)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Cache bust CSS ?v=5 y JS ?v=4 en index.html</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (buscar líneas con components.css y app.js para ver versiones actuales)
  </read_first>
  <action>
En `public/index.html`:

**Cambio 1 — bump CSS**: Reemplazar `components.css?v=4` por `components.css?v=5`
(Si la versión actual es diferente, buscar el patrón `components.css?v=` y cambiar el número a 5)

**Cambio 2 — bump JS**: Reemplazar `app.js?v=3` por `app.js?v=4`
(Si la versión actual es diferente, buscar el patrón `app.js?v=` y cambiar el número a 4)

Solo modificar estas dos referencias. No tocar otras referencias CSS/JS.
  </action>
  <verify>
    <automated>grep -E "components\.css\?v=|app\.js\?v=" public/index.html</automated>
  </verify>
  <acceptance_criteria>
    - grep 'components.css' public/index.html contiene ?v=5
    - grep 'app.js' public/index.html contiene ?v=4
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Refresh dateModified en schemas y article:modified_time (home + landings)</name>
  <files>public/index.html, public/vacaciones-invierno-2026.html, public/feriados-2026.html, public/cuando-empiezan-clases-2026.html, public/about.html</files>
  <read_first>
    - public/index.html (buscar "dateModified" en bloque JSON-LD @graph — ver valor actual)
    - public/vacaciones-invierno-2026.html (buscar article:modified_time y dateModified)
    - public/feriados-2026.html (buscar article:modified_time y dateModified)
    - public/cuando-empiezan-clases-2026.html (buscar article:modified_time y dateModified)
    - public/about.html (buscar article:modified_time y dateModified)
  </read_first>
  <action>
Actualizar las fechas de modificación en todos los archivos:

**public/index.html**:
- Buscar `"dateModified": "2026-04-` (en el bloque JSON-LD @graph → WebSite) → cambiar a `"dateModified": "2026-04-23"`
- Si hay múltiples dateModified en el JSON-LD, actualizar TODOS a 2026-04-23

**public/vacaciones-invierno-2026.html**:
- `<meta property="article:modified_time"` → cambiar content a `2026-04-23T00:00:00-03:00`
- Si existe `"dateModified"` en JSON-LD → cambiar a `2026-04-23`

**public/feriados-2026.html**:
- `<meta property="article:modified_time"` → cambiar content a `2026-04-23T00:00:00-03:00`
- Si existe `"dateModified"` en JSON-LD → cambiar a `2026-04-23`

**public/cuando-empiezan-clases-2026.html**:
- `<meta property="article:modified_time"` → cambiar content a `2026-04-23T00:00:00-03:00`
- Si existe `"dateModified"` en JSON-LD → cambiar a `2026-04-23`

**public/about.html**:
- `<meta property="article:modified_time"` (si existe) → cambiar content a `2026-04-23T00:00:00-03:00`
- Si existe `"dateModified"` en JSON-LD → cambiar a `2026-04-23`
  </action>
  <verify>
    <automated>grep -r "dateModified.*2026-04" public/index.html public/vacaciones-invierno-2026.html public/feriados-2026.html public/cuando-empiezan-clases-2026.html public/about.html 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - grep 'dateModified' public/index.html contiene "2026-04-23"
    - grep 'dateModified\|modified_time' public/vacaciones-invierno-2026.html contiene 2026-04-23 (si existe)
    - grep 'dateModified\|modified_time' public/feriados-2026.html contiene 2026-04-23 (si existe)
    - grep 'dateModified\|modified_time' public/cuando-empiezan-clases-2026.html contiene 2026-04-23 (si existe)
    - Ninguno de los archivos modificados tiene dateModified con fecha anterior a 2026-04-23
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `grep '<title>' public/index.html` — debe contener "Fechas Oficiales por Región"
2. `grep -c 'href="/region/' public/index.html` — debe retornar ≥16
3. `grep 'home-stats' public/index.html` — debe retornar 1 match (sección presente)
4. `grep 'initHomeStats' public/js/app.js` — debe retornar ≥2 matches (definición + llamada)
5. `grep '.region-index' public/css/components.css` — debe retornar ≥1 match
6. `grep '.home-stats' public/css/components.css` — debe retornar ≥1 match
7. `grep 'components.css?v=5' public/index.html` — debe retornar 1 match
8. `grep 'app.js?v=4' public/index.html` — debe retornar 1 match
9. `grep 'dateModified.*2026-04-23' public/index.html` — debe retornar ≥1 match
10. `grep 'id="region-select"' public/index.html` — debe retornar 1 match (select NO fue eliminado)
</verification>

<success_criteria>
- Home title revertido al patrón ranker histórico "Fechas Oficiales por Región"
- 16 anchor links crawlables a páginas de región restaurados
- Stats bar con semana escolar, días a vacaciones y feriados restantes presente y funcional
- dateModified actualizado a 2026-04-23 en todos los schemas
- CSS y JS con cache bust correcto
</success_criteria>

<output>
Al completar, crear `.planning/phases/15-seo-recovery/15-02-SUMMARY.md`
</output>
