---
phase: 15-seo-recovery
plan: 03
type: execute
wave: 2
depends_on:
  - "15-01"
  - "15-02"
files_modified:
  - BLUEPRINT.md
autonomous: true
requirements:
  - SEO-B7

must_haves:
  truths:
    - "npm run generate sale exit code 0"
    - "node scripts/validate.js sale exit code 0"
    - "public/sitemap.xml generado no contiene .html en las <loc> (excepto región paths que ya usan trailing slash)"
    - "BLUEPRINT.md contiene sección 'SEO Recovery v3 — Core Update response (2026-04-23)'"
    - "BLUEPRINT.md línea 'Ultimo update' dice 2026-04-23"
  artifacts:
    - path: "BLUEPRINT.md"
      provides: "Registro de diagnóstico, fixes aplicados, baseline y gate de escalación SEO Recovery v3"
      contains: "SEO Recovery v3"
    - path: "public/sitemap.xml"
      provides: "Sitemap regenerado con URLs sin .html"
      contains: "<loc>https://calendarioescolar.cl/vacaciones-invierno-2026</loc>"
  key_links:
    - from: "scripts/generate-pages.js"
      to: "public/sitemap.xml"
      via: "genera sitemap"
      pattern: "sitemap"
---

<objective>
Regenerar todos los artefactos estáticos (16 páginas región, sitemap, regions-data.js, calendar-config.js, health.json), validar integridad de datos, y documentar el SEO Recovery v3 en BLUEPRINT.md.

Este plan es Wave 2 — depende de que los planes 01 y 02 hayan completado sus fixes antes de regenerar, para que el sitemap resultante ya incluya las URLs sin .html.

Output: artefactos regenerados con el fix de sitemap aplicado, validate.js en exit 0, BLUEPRINT.md actualizado con diagnóstico, fixes, baseline y gate de escalación.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerar artefactos estáticos</name>
  <files>public/sitemap.xml, public/js/regions-data.js, public/js/calendar-config.js, public/health.json, public/region/*/index.html</files>
  <read_first>
    - scripts/generate-pages.js (líneas 1-20 — ver qué archivos genera y el array PAGES)
    - data/pages.json (primeras 5 líneas — verificar que el archivo existe y es válido JSON)
    - data/calendar-config.json (primeras 5 líneas — verificar que existe)
  </read_first>
  <action>
Ejecutar:
```bash
npm run generate
```

El script debe producir sin errores:
- `public/sitemap.xml` (con URLs sin .html en las <loc> de páginas planas)
- `public/js/regions-data.js`
- `public/js/calendar-config.js`
- `public/health.json`
- `public/region/*/index.html` (16 archivos, uno por región)

Si el comando falla, leer el error completo antes de intentar cualquier fix. Si falla por un issue nuevo no relacionado con este plan, detener y reportar al usuario.
  </action>
  <verify>
    <automated>npm run generate 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm run generate sale exit code 0
    - public/sitemap.xml existe
    - public/health.json existe
    - public/js/regions-data.js existe
    - ls public/region/ muestra 16 directorios de región
    - grep 'vacaciones-invierno-2026.html' public/sitemap.xml retorna 0 resultados (sin .html en loc)
    - grep 'vacaciones-invierno-2026<' public/sitemap.xml retorna 1 resultado (loc sin .html presente)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Validar integridad de datos</name>
  <files></files>
  <read_first>
    - scripts/validate.js (primeras 20 líneas — ver qué valida)
  </read_first>
  <action>
Ejecutar:
```bash
node scripts/validate.js
```

El script debe salir con exit code 0. Warnings sobre AdSense placeholder son conocidos y aceptables (BUG 6 documentado en BLUEPRINT.md).

Si hay ERRORs (no warnings) en la salida, leer el mensaje completo, identificar el archivo de datos involucrado, y corregir el issue antes de continuar. No ignorar errores de integridad de datos.
  </action>
  <verify>
    <automated>node scripts/validate.js; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - node scripts/validate.js sale exit code 0
    - La salida del script no contiene la cadena "ERROR" (warnings son OK)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Actualizar BLUEPRINT.md con SEO Recovery v3</name>
  <files>BLUEPRINT.md</files>
  <read_first>
    - BLUEPRINT.md (leer completo — identificar: línea "Ultimo update", sección existente de SEO recovery si hay alguna, y dónde insertar la nueva sección)
  </read_first>
  <action>
En `BLUEPRINT.md`:

**Cambio 1 — actualizar línea "Ultimo update"**: Buscar la línea que contiene "Ultimo update de este blueprint" (o similar) y cambiar la fecha a `2026-04-23`.

**Cambio 2 — agregar sección SEO Recovery v3**: Insertar después de la sección de SEO Recovery más reciente que exista (buscar "SEO Recovery" en el archivo). Si no hay ninguna, agregar al final. El bloque a insertar:

```markdown
---

## SEO Recovery v3 — Core Update response (2026-04-23)

### Diagnóstico
Caída de 3000 → 5 impresiones/día (2026-04-13 → 2026-04-21). Posición 2.8 → 32.6.
Causa raíz: **Google March 2026 Core Update** (rollout 27-mar → 8-abr, tail 13-15 abril).
El update explícitamente movió visibilidad away from aggregators/utility pages toward destination sources.
calendarioescolar.cl es aggregator de datos Mineduc → target directo.

### Fixes aplicados
- **B1** Canonical + hreflang: 4 landings ahora apuntan a URL sin `.html` (alineado con pretty URLs Cloudflare).
- **B2** Sitemap: `scripts/generate-pages.js` emite `<loc>` sin `.html`.
- **B3** Title revert: "Calendario Escolar 2026 Chile — Fechas Oficiales por Región" (patrón ranker histórico).
- **B4** H2 semántico "¿De qué región eres?" + `<section class="region-index">` con 16 anchors a páginas región.
- **B5** `<section class="home-stats">` con semana escolar / días a vacaciones / feriados restantes (contenido único time-sensitive → señal destination-source).
- **B6** `dateModified` refrescado a 2026-04-23 en schemas + article:modified_time.

### Baseline 2026-04-23
- Impresiones: ~5/día
- Clicks: 0/día
- Posición promedio: 32.6
- Pages top (histórico pre-caída): `/vacaciones-invierno-2026` (14855 impr), `/` (9460 impr), `/feriados-2026` (1954 impr)
- Query motor: "vacaciones de invierno 2026" (7009 impr acumulado, pos 3.43 histórico)

### Gate de escalación
- **2026-04-30 (7d)**: ≥50 impr/día + pos <15 → recovery iniciándose.
- **2026-05-07 (14d)**: ≥500 impr/día + pos <10 → trayectoria OK.
- **2026-05-14 (21d)**: si sigue <200 impr/día → escalar:
  - Reintroducir tabla feriados con badges BCN SHA256 en home.
  - Considerar nueva landing "calendario-escolar-2026" como destination page con datos densos.
  - Revisar reversión parcial del refactor ce2835f.

### Track C manual pendiente (user)
- [ ] GSC → Security & Manual actions (descartar penalización)
- [ ] GSC → Pages coverage: exportar y revisar URLs "Crawled - not indexed" entre 10-21 abril
- [ ] GSC → URL Inspection en: `/`, `/vacaciones-invierno-2026`, `/feriados-2026`, top 3 regiones
- [ ] GSC → Sitemaps → re-submit `sitemap.xml` tras deploy
- [ ] GSC → URL Inspection → Request Indexing en las URLs top post-deploy
- [ ] Cloudflare Analytics → descartar spike 4xx/5xx entre 13-15 abril
```
  </action>
  <verify>
    <automated>grep -c "SEO Recovery v3" BLUEPRINT.md</automated>
  </verify>
  <acceptance_criteria>
    - grep 'SEO Recovery v3' BLUEPRINT.md retorna ≥1 match
    - grep '2026-04-23' BLUEPRINT.md retorna ≥2 matches (línea update + baseline)
    - grep 'Track C' BLUEPRINT.md retorna ≥1 match
    - grep 'Gate de escalación' BLUEPRINT.md retorna ≥1 match
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `npm run generate && echo OK` — debe imprimir OK (exit 0)
2. `node scripts/validate.js && echo OK` — debe imprimir OK (exit 0)
3. `grep 'vacaciones-invierno-2026.html' public/sitemap.xml` — debe retornar 0 resultados (sin .html)
4. `grep 'vacaciones-invierno-2026<' public/sitemap.xml` — debe retornar 1 resultado (<loc> sin .html)
5. `grep 'SEO Recovery v3' BLUEPRINT.md` — debe retornar ≥1 match
6. `grep 'generatedDate.*2026-04-23' public/health.json` — debe retornar 1 match (health.json actualizado hoy)

**Post-deploy manual (Track C — ejecutar tras push a main)**:
- GSC Security & Manual actions → verificar sin penalización
- GSC Sitemaps → re-submit sitemap.xml
- URL Inspection + Request Indexing en /, /vacaciones-invierno-2026, /feriados-2026, /region/metropolitana/, /region/valparaiso/, /region/maule/
- Cloudflare Analytics → verificar sin spike 4xx/5xx en 13-15 abril
</verification>

<success_criteria>
- Artefactos regenerados con sitemap sin .html en <loc>
- validate.js pasa sin errores
- BLUEPRINT.md registra el SEO Recovery v3 con diagnóstico, fixes, baseline y gate de escalación
- health.json tiene generatedDate de hoy
</success_criteria>

<output>
Al completar, crear `.planning/phases/15-seo-recovery/15-03-SUMMARY.md`
</output>
